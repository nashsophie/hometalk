<p>
	<a href="<%=@post.url %>"> <%= @post.title %></a>
</p>

<p>
	All <%= @post.comments.count %> comments
	<ul>
		<% @post.comments.each do |comment| %>
		<li>
			<%= comment.content %>
		</li>
		<% end %>
	</ul>
</p>
